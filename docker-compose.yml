version: '3.8'

services:
  cloudmulti:
    build:
      context: .
      dockerfile: Dockerfile
    image: cloudmulti:latest
    expose:
      - "8080"
    environment:
      - PORT=8080
      - DATABASE_URL=sqlite:///app/data/cloudflare.db?mode=rwc
      - RUST_LOG=info,cloudflare_dashboard_backend=debug
    dns:
      - 1.1.1.1
      - 8.8.8.8
    volumes:
      - ./data:/app/data
      - cloudmulti-data:/app/data
    restart: always

volumes:
  cloudmulti-data:
    driver: local
